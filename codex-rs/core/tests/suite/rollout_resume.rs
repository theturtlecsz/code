// SPEC-957: This entire test file uses private/internal APIs that are no longer accessible
// Including: rollout module, RolloutRecorderParams, rollout_path field
// Tests are stubbed out until internal test infrastructure is available

#[tokio::test]
#[ignore = "SPEC-957: requires private rollout module and RolloutRecorderParams"]
async fn resume_restores_recorded_events() {
    unimplemented!("Test uses private APIs: rollout module, RolloutRecorderParams, rollout_path")
}
