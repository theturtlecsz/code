{
  "name": "code-monorepo",
  "private": true,
  "description": "Tools for repo-wide maintenance.",
  "scripts": {
    "format": "prettier --check *.json *.md .github/workflows/*.yml **/*.js",
    "format:fix": "prettier --write *.json *.md .github/workflows/*.yml **/*.js",
    "build": "cd codex-rs && cargo build --release --bin code --bin code-tui --bin code-exec",
    "build:dev": "cd codex-rs && cargo build --bin code --bin code-tui --bin code-exec",
    "build:quick": "cd codex-rs && cargo build --release --bin code",
    "test:local": "cd codex-cli && npm link && code --version",
    "fix": "cargo fmt && cargo fix --allow-dirty && cargo clippy --fix --allow-dirty"
  },
  "devDependencies": {
    "fast-glob": "^3.3.3",
    "gray-matter": "^4.0.3",
    "prettier": "^3.6.2",
    "remark": "^15.0.1",
    "remark-frontmatter": "^5.0.0",
    "remark-gfm": "^4.0.1",
    "remark-toc": "^9.0.0",
    "remark-validate-links": "^13.1.0",
    "simhash-js": "^1.0.0",
    "typescript": "^5.9.2",
    "unified": "^11.0.5"
  },
  "resolutions": {
    "braces": "^3.0.3",
    "micromatch": "^4.0.8",
    "semver": "^7.7.1"
  },
  "overrides": {
    "punycode": "^2.3.1"
  },
  "engines": {
    "node": ">=22",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@10.8.1"
}
