The next session will focus on downstream repos (notebooklm-mcp, localmemory-policy). Should we add any codex-rs refinements first?

❯ 1. [ ] Start downstream work
     Focus on notebooklm-mcp doctor command and localmemory-policy updates
  2. [ ] Add pointer recall command
     Add `code stage0 recall SPEC-ID` before moving on
  3. [ ] Add more tests first
     Expand convergence test coverage before downstream work
  4. [ ] Type something
     Submit
