# SPEC-947 (Archived)

This directory contains historical `pipeline.toml` artifacts.

`SPEC.md` is the canonical tracker; this directory is not currently part of the active tables, but is retained for reference.
